import "tfconfig"

param region default "us-east-1"
aws = tfconfig.providers.aws
aliases = aws["alias"]
print(aliases)


//main = rule { tfconfig.providers.aws.alias.default.config.region matches region }
main = rule { 1 is 1 }
